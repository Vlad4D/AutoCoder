# Normalize line endings: store text as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Windows batch files must stay CRLF to run correctly.
*.bat text eol=crlf

# Binary assets — never normalize or diff as text.
*.png binary
*.ico binary
*.bmp binary
*.qrc text eol=lf
