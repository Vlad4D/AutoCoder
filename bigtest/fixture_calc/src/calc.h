#pragma once

// Sum of all integers in the inclusive range [a, b]. Returns 0 when a > b.
int sumRange(int a, int b);

// Largest of the first n entries of values (n must be >= 1).
int maxOf(const int* values, int n);
